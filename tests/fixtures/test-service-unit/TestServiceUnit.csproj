<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>test-service-unit</AssemblyName>
    <IsPackable>false</IsPackable>
    <Description>Minimal fixture process for the A3 Service Process Gate: writes heartbeats and serves a named-pipe readiness probe.</Description>
  </PropertyGroup>

</Project>
