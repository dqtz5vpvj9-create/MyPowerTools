# Production Readiness Evidence

This document tracks the objective criteria against repository evidence. It is intentionally strict: an item remains open until current code, tests, runtime behavior, release artifacts, or docs prove it.

## Verified In This Iteration

| Criterion | Evidence |
|---|---|
| .NET SDK 10 locked | `global.json` pins SDK `10.0.301`; `dotnet --version` returns `10.0.301`; projects target `net10.0`. |
| Runner / Shell split exists | `src/MyPowerTools.Runner` and `src/MyPowerTools.Shell.Avalonia` are separate processes. |
| Runner tray exists | `ITrayService` defines tray options, actions, and startup results; `WindowsTrayService` uses native `Shell_NotifyIcon` with Open Shell and Quit Runner actions; Runner starts the tray in background mode and supports `--no-tray` for headless execution. macOS/Linux expose explicit unsupported tray services. `Unsupported_tray_service_reports_actionable_degraded_state` verifies degraded tray behavior. |
| Typed protocol exists | `proto/mpt_module_v1.proto` and `proto/mpt_host_control_v1.proto` generate C# gRPC contracts. |
| Package/module schema validation exists | `SchemaPackageValidator` validates package, module, and UI surface JSON. |
| T0 manifest and command indexing exists | `PackageRegistry`, `StaticCommandIndexReader`, and `CommandIndex` index modules and declared `commands.index.json` entries without starting sidecars. |
| Settings revision conflict exists | `SettingsStore` enforces expected revision and persists snapshots with rollback backup. |
| Event seq exists | `EventBus` assigns monotonic sequence values. |
| Command invocation idempotency exists | `MptHostRuntime` caches command execution results by `invocationId`. |
| Static command execution exists | `MptHostRuntime.ExecuteCommandAsync` handles declared `open`, `host.*`, `http.request`, and broker-request command descriptors. |
| Dynamic transport execution exists | `IModuleTransportRuntime`, `RefreshDynamicCommandsAsync`, and Runner/CLI transport injection execute module commands through InProc, gRPC IPC, and stdio hosts. |
| gRPC native IPC sidecar path exists | `Runtime_runs_grpc_sidecar_module_over_native_ipc` starts the sample sidecar over native IPC, imports dynamic commands, and executes `sample.grpc.ping`. |
| gRPC sidecar crash recovery exists | `Runtime_recovers_grpc_sidecar_after_process_crash` forces the sample sidecar to exit, then verifies Runtime restarts the sidecar, executes `sample.grpc.ping` again, observes a different process id, and records invocation/event correlation in logs. |
| gRPC sidecar restart policy exists | `Grpc_ipc_runtime_enforces_restart_limit_after_crash_loop` verifies the 30-second restart window and 4-start limit return `MPT_RUNTIME_UNAVAILABLE` with a diagnostic `restart limit reached` message after a crash loop. |
| Runtime process restart control exists | Runtime exposes `RestartRuntimeProcessAsync`, HostControl exposes `RestartRuntimeProcess`, Shell Diagnostics renders a Restart action for transport processes, and CLI exposes `mpt runner process restart <transport-kind> <pool-key>`. `Runtime_restarts_grpc_process_pool_on_request`, `HostControl_restarts_grpc_process_pool`, and `Cli_restarts_runner_grpc_process_pool_over_hostcontrol` verify the behavior. |
| Runtime process restart policy controls exist | Runtime exposes `SetRuntimeProcessRestartPolicyAsync`, HostControl exposes `SetRuntimeProcessRestartPolicy`, Shell Diagnostics renders Pause/Resume actions with policy reason text, and CLI exposes `mpt runner process pause|resume <transport-kind> <pool-key> [--reason <reason>]`. `Runtime_pauses_and_resumes_grpc_restart_policy`, `HostControl_sets_grpc_process_restart_policy`, and `Cli_restarts_runner_grpc_process_pool_over_hostcontrol` verify paused-pool diagnostics, blocked sidecar restarts, and recovery after resume. |
| Runtime process restart policy persistence exists | `RuntimeProcessPolicyStore` persists current non-automatic policies and the last 100 policy history entries under `state/runtime.process-policies.json`; Runtime registers known process pools on `Load`, reapplies paused policies before sidecar startup, exposes source-aware history through RuntimeDiagnostics, and `Runtime_persists_grpc_restart_policy_across_runtime_reload` verifies paused policy restoration and recovery after resume. |
| Runtime process maintenance windows exist | `SetRuntimeProcessRestartPolicy` carries typed `expires_at`, CLI `mpt runner process pause` accepts `--until` and `--duration-minutes`, Shell Diagnostics exposes `Pause 1h`, Runtime expires elapsed policies before diagnostics, health refresh, dynamic command refresh, restart, and command execution, then records an automatic `runtime.expiry` history entry. `Runtime_expires_grpc_restart_policy_and_recovers_pool`, `HostControl_sets_grpc_process_restart_policy`, and `Cli_restarts_runner_grpc_process_pool_over_hostcontrol` verify the path. |
| HTTP facade integration exists | `Runtime_executes_http_facade_module_against_local_service` starts a local TCP HTTP facade, refreshes module health through the HTTP entrypoint, executes a static `http.request` command, verifies `token=****` redaction, and checks command log correlation. |
| Concurrent log append is resilient | `LogRouter` serializes in-process writes per log file, uses read-shared/exclusive writer file handles with retry for cross-process contention, skips blank tail records, and `Log_router_allows_parallel_append_to_same_module_file` verifies concurrent append behavior. |
| Package shared runtime pool exists | `Runtime_pools_package_runtime_sidecar_across_modules` verifies two modules using the same `shared.runtimes` gRPC entrypoint execute through the same sidecar process id and exposes a single runtime process diagnostic row with pool key, PID, endpoint, restart budget, last start time, and both module ids. |
| AndroidTools shared runtime facade exists | `AndroidTools.MyPowerTools` is a real InProc module assembly shared by `android-tools.notifications`, `android-tools.remote-commands`, and `android-tools.process-monitor`. Remote Commands imports the packaged powertool `commands.yaml` into dynamic MPT commands, executes migrated text tools, writes shared history, Notifications imports endpoint config and probes the server, and Process Monitor persists/scans a shared watch list. |
| AndroidTools powertoold T2 sidecar exists | `src/AndroidTools.Powertoold` hosts the three AndroidTools modules through the typed ModuleControl gRPC service over Windows named pipe `mypowertools.android-tools-suite.powertoold`. AndroidTools manifests prefer `package-runtime:100`; InProc remains a fallback. `AndroidTools_powertoold_imports_powertool_commands_and_executes_text_tool` and `AndroidTools_powertoold_process_monitor_persists_shared_watch_list` verify T2 dynamic commands, argument forwarding, shared runtime process diagnostics, and process watch-list persistence. |
| AndroidTools degraded paths are tested | `AndroidTools_notifications_reports_actionable_degraded_state_when_endpoint_config_is_invalid` verifies invalid notification endpoint config returns degraded status and actionable server-check output. `AndroidTools_process_monitor_reports_actionable_degraded_state_when_watch_list_is_empty` verifies an empty watch list stays degraded, reports zero configured processes, and rejects empty saves with `MPT_VALIDATION_FAILED`. |
| Doubao Agent controller facade exists | `DoubaoAgent.MyPowerTools` is a real InProc module copied into `modules/doubao-agent`. It checks planner port 38102, tool runtime port 38080, and MCP bridge port 38189 separately, exposes status summary, per-role health commands, self-test, log summary, runtime settings schema, and degraded state when only part of the runtime is reachable. `DoubaoAgent_inproc_module_reports_planner_tool_and_mcp_services` verifies local three-service probes, dynamic commands, redaction, and log summary behavior. |
| Doubao role degradation is tested | `DoubaoAgent_inproc_module_reports_role_specific_degraded_services` verifies role-specific partial outage output when planner is reachable while tool runtime and MCP are unavailable, including retryable `MPT_RUNTIME_UNAVAILABLE` details for the failed role command. |
| SmartBird Thermostat typed facade exists | `SmartBirdThermostat.MyPowerTools` is a real InProc module copied into `modules/smartbird-thermostat`. It wraps the existing HTTP status/events/config/log endpoints, exposes config save, self-test, log summary, bounded event output, ServiceBroker restart request details, runtime settings schema, and degraded Energy Server/FNB-58/ADB diagnostics. `SmartBird_inproc_module_reports_facade_config_and_hardware_degradation` verifies local facade probes, config persistence, brokered restart, redaction, and hardware degradation behavior. |
| ScreenEase profile facade and display writer exist | `ScreenEase.MyPowerTools` is a real InProc module copied into `modules/screenease`. It exposes dynamic status, display enumeration, profile list/plan/apply/save, native writer status/configure, and rule status commands. Windows `IDisplayService` enumerates monitors through Win32, probes DDC/CI through Dxva2, and applies brightness/color-temperature profile intents when hardware writes are explicitly requested or enabled. macOS/Linux providers compile with explicit degraded responses. Unsupported monitors return actionable hardware-level diagnostics instead of fake success. |
| AdbForwarder production diagnostics exist | `AdbForwarder.MyPowerTools` is a real InProc module copied into `modules/adb-forwarder`; `AdbForwarder_inproc_module_collects_diagnostics` verifies dynamic diagnostics commands and non-unsupported status. |
| AdbForwarder port mapping model exists | `AdbPortProxyModel`, `PortProxyParser`, and `AdbPortProxyPlanner` parse Windows `netsh portproxy` state, validate configured mappings, plan apply/revert changes, and emit rollback steps. `AdbForwarder_apply_request_returns_broker_plan` verifies the dynamic `adb-forwarder.portproxy.apply` command returns a detailed `NetworkBroker` permission request with `expectedChange` and `rollback`. |
| NetworkBroker portproxy execution path exists | `NetworkBroker.ApplyChangeSetAsync` executes remove/apply change sets through `PrivilegedBroker` evaluation and `INetworkBroker`, audits every elevated step, and runs rollback after partial failure. `WindowsNetworkBroker` executes `netsh.exe interface portproxy add/delete` when the process has an administrator token and returns `permission-required` otherwise. |
| Shell broker permission visibility exists | HostControl `ExecuteCommand` now returns typed error code/message/details and `ListBrokerAudit` returns recent audit entries. Shell shows `permission-required` action, scope, reason, expected change counts, rollback count, and latest Broker Audit entries in the command side panel. `HostControl_execute_command_exposes_permission_details` and `HostControl_lists_broker_audit_entries` verify the IPC contract. |
| Module permission visibility exists | `ModuleSummary` and `ModuleDetail` carry typed `ModulePermission` and `ModuleRequirement` records through HostControl; Shell module cards show permission counts and module detail pages render declared permissions plus capability requirements; CLI `mpt inspect modules` prints capabilities, required/optional requirements, and broker permissions. `HostControl_module_detail_exposes_declared_permissions` and `Cli_inspect_package_hash_and_doctor_cover_package_commands` verify the path. |
| Shell multi-page IPC surfaces exist | `src/MyPowerTools.Shell.Avalonia/MainWindow.cs` renders Dashboard, Modules, Settings, Logs, Notifications, Packages, and Diagnostics pages through `HostControlClient`. Settings save through `UpdateSettings` with revision protection, logs stream through `TailLogs`, and module detail pages use `GetModuleDetail`. |
| Shell HostControl reconnection exists | `HostControlConnectionMonitor` continuously probes Runner HostControl, tracks offline failure counts, marks recovery, and MainWindow refreshes the current page plus command/audit panels after reconnection. `Shell_connection_monitor_reports_offline_then_restored` verifies the offline-to-restored transition. |
| Shell HostControl event stream consumption exists | `HostControlClient.SubscribeHostEventsAsync` reads typed Runner events, `HostControlEventStreamMonitor` tracks `lastEventSeq`, skips replayed events, reports stream faults, and reconnects. `MainWindow` starts the monitor and refreshes notifications, settings, commands, dashboard, modules, packages, diagnostics, and broker audit data from Runner snapshots based on event type. `Shell_event_stream_monitor_resumes_after_fault_and_tracks_seq` verifies resume, fault reporting, and duplicate event filtering. |
| Runtime diagnostics IPC exists | `mpt_host_control_v1.proto` exposes `GetRuntimeDiagnostics`, `RestartRuntimeProcess`, and `SetRuntimeProcessRestartPolicy`; Runtime reports versions, protocols, platform, paths, module/command/event counts, transport registration, active transport process pools, restart policy, policy reason, policy expiry, source-aware process policy history, module states, and recent command history. Shell Diagnostics and CLI `mpt diagnostics` use the typed snapshot. `HostControl_returns_runtime_diagnostics_snapshot`, `HostControl_returns_grpc_process_diagnostics_snapshot`, `Cli_diagnostics_reports_runtime_snapshot`, and `Cli_diagnostics_reports_grpc_process_snapshot` verify the paths. |
| Shell HostControl smoke exists | `src/MyPowerTools.Shell.Avalonia/Program.cs` supports `--smoke --timeout-ms`; it connects to Runner through `HostControlClient`, pings Runner, reads Dashboard, modules, and commands, then exits with CI-friendly status. |
| Runner graceful shutdown IPC exists | HostControl `QuitRunner` is wired to `IHostApplicationLifetime.StopApplication`; Shell smoke can pass `--quit-runner`, and `scripts/smoke.ps1` waits for the smoke-owned Runner process to exit before using forceful cleanup. |
| Runner autostart path exists | `WindowsAutostartService` manages the current-user HKCU Run entry through `IAutostartService`; `AutostartBroker` audits status, enable, and disable actions; CLI `mpt runner autostart status|enable|disable` exposes the path. `AutostartBroker_enable_disable_status_audits_user_actions` verifies broker audit coverage, and `Cli_runner_autostart_status_reports_missing_entry` verifies the CLI path without modifying registry state. |
| Notification Center HostControl surface exists | `mpt_host_control_v1.proto` exposes `ListNotifications`; `HostControlGrpcService` maps `NotificationCenter.List()` to typed `NotificationItem` records; `HostControl_lists_notification_center_entries` verifies Shell-readable notifications. |
| Package Manager HostControl surface exists | `mpt_host_control_v1.proto` exposes `ListPackages`, `InstallPackage`, `RepairPackage`, `UninstallPackage`, and `RollbackPackage`; Runtime returns package id, display name, version, hash manifest, trust state, trust policy, signature path, trust issue count, shared runtime count, module count, and module ids; Shell Packages page renders trust state, signature path, install input, repair, uninstall, and rollback actions. `HostControl_lists_package_summaries` verifies the multi-module AndroidTools package summary and trust fields; `HostControl_package_operations_reload_runtime_store` verifies install, repair, uninstall, rollback, runtime reload, and package counts. |
| Module enable/disable persistence exists | `ModuleStateStore` persists disabled module ids under `state/modules.enabled.json`; Runtime filters disabled modules out of Dashboard and command indexes, HostControl exposes `SetModuleEnabled`, CLI exposes `mpt module list/enable/disable`, and the Shell Modules page can toggle state. `Runtime_persists_module_enable_disable_state`, `HostControl_set_module_enabled_updates_runtime_state`, and `Cli_module_enable_disable_persists_state` verify the paths. |
| UI contract and pixel snapshots exist | `mpt ui snapshot` scans real module UI surfaces and writes `artifacts/ui-snapshots/ui-snapshot-manifest.json` plus paired per-surface `.snapshot.json` and `.snapshot.png` files with layout, component usage, state coverage, theme, density, size, source SHA256, pixel SHA256, dimensions, unique-color counts, and nonblank pixel counts. `Ui_snapshot_writes_contract_manifest` verifies the manifest, PNG signature, dimensions, hash fields, and nonblank image metrics. |
| Shell UI snapshot matrix exists | `mpt ui shell-snapshot` writes `artifacts/shell-ui-snapshots/shell-ui-snapshot-manifest.json` plus PNG snapshots for Dashboard, Command Palette, Settings Center, Module Detail, Logs Viewer, Notification Center, Permission Prompt, Degraded Module, Package Manager, and Runtime Diagnostics. `Ui_shell_snapshot_writes_key_surface_matrix` verifies all required Shell surfaces, PNG signatures, dimensions, hashes, and nonblank image metrics. |
| Shell keyboard and focus evidence exists | `ShellKeyboardShortcut` maps Command Palette focus, clear, refresh, and `Ctrl+1..7` page navigation; MainWindow applies those shortcuts. Shell snapshot manifests include 12 shortcut entries and 7 focus states. `Shell_keyboard_shortcuts_resolve_navigation_and_command_palette_actions` verifies the shortcut model. |
| Shell Settings is schema-rendered through HostControl | HostControl exposes `GetSettingsSchema`, Runtime retrieves schemas through InProc, gRPC IPC, and stdio transport hosts, and Shell Settings renders booleans, enums, scalar fields, and JSON object/array editors from runtime schema before saving through revision-checked HostControl updates. `HostControl_get_settings_schema_exposes_runtime_schema` verifies the IPC contract. |
| Shell colors are centralized | `MptTheme` owns Shell/control palette brushes; MainWindow and MPT controls no longer contain direct `Brush.Parse("#...")` or `Brushes.White` literals. `Shell_ui_colors_are_centralized_in_theme_tokens` verifies the guard. |
| CLI process test exists | `Cli_validate_modules_returns_successful_exit_code` runs the CLI as a child process and verifies `mpt validate modules` returns exit code 0. |
| Module contract gate exists | `ModuleContractValidator` backs `mpt validate contracts` and verifies schema validity, dashboard/settings/log surfaces, indexed commands, runtime dashboard presence, typed health state, runtime settings schema, and log provider availability for every production module. `Cli_validate_contracts_reports_all_module_contracts` verifies the CLI path as a child process. |
| Broader CLI coverage exists | `Cli_inspect_package_hash_and_doctor_cover_package_commands` verifies `mpt inspect modules`, `mpt package hash <package-dir>`, `mpt package sign-local <package-dir>`, `mpt package trust <package-dir> --strict`, and `mpt doctor` as child processes. |
| Broker audit foundation exists | `AuditLog`, `PrivilegedBroker`, `ServiceBroker`, `NetworkBroker`, `SecretBroker`, and `AutostartBroker` append audit records. |
| Planned permission levels are enforced | `schemas/module.schema.json` accepts `user`, `elevated`, `service`, `serviceUser`, `serviceSystem`, `sensitive`, and `broker`; `PrivilegedBroker` treats all non-user planned privilege levels as broker-required. `Module_schema_accepts_planned_permission_levels` and `PrivilegedBroker_requires_broker_for_planned_privilege_levels` verify schema and runtime decisions. |
| ServiceBroker uses serviceUser audit level | `ServiceBroker.RestartAsync` writes `serviceUser` audit entries for user-level service restarts. `ServiceBroker_restart_audits_service_user_level` verifies requested/start entries, rollback text, operation order, and redaction. |
| OS secret store path exists | `ISecretStore` is the platform abstraction; `WindowsCredentialSecretStore` stores module secrets in Windows Credential Manager and returns only `secret://module/name` references; `InMemorySecretStore` covers tests; macOS/Linux expose unsupported degraded secret providers. `SecretBroker_stores_reads_deletes_and_redacts_audit`, `InMemorySecretStore_rejects_unsafe_secret_references`, and CLI `mpt broker secret self-test` verify save/read/delete and redacted audit behavior without printing secret values. |
| Package hash foundation exists | `PackageIntegrity` writes and verifies sha256 manifests; `mpt package sign-local modules` refreshed all 5 production package manifests. |
| Local package trust hooks exist | `PackageTrustVerifier` validates hash manifests plus `package.signature.json`; `mpt package sign-local modules` writes the hook, `mpt package trust modules --strict` verifies all 5 production packages, `PackageStore.Install` verifies source packages before copying, and `PackageStore.Repair` re-runs trust verification. `Package_trust_strict_policy_requires_signature_hook`, `Package_store_installs_and_repairs_hashes`, and `Cli_inspect_package_hash_and_doctor_cover_package_commands` cover the path. |
| Package rollback path exists | `PackageStore.Uninstall` preserves the installed package as `.rollback`, `PackageStore.Rollback` restores it, `PackageReader.DiscoverPackages` excludes rollback backups from active runtime discovery, CLI `mpt rollback <package-id> [--store-root <dir>]` exposes the path, and HostControl exposes rollback for Shell Package Manager. `Package_store_uninstall_and_rollback_restores_installed_package`, `Cli_package_install_uninstall_rollback_uses_store_root`, and `HostControl_package_operations_reload_runtime_store` verify install, uninstall, rollback, repair, and active package discovery using isolated package stores. |
| Platform packs exist | Windows provider plus macOS/Linux compile-ready degradation providers are present. |
| UI component foundation exists | `MyPowerTools.UI.Controls` defines reusable MPT controls. |
| Production module root excludes sample modules | `modules/` contains 5 production packages and 7 real tool modules; sample modules live under `tests/fixtures/modules/`. |
| HTTP facade health checks exist | `HealthMonitor` refreshes HTTP module state before Dashboard snapshots and `Runner --once`. |
| Command Palette data exists | Real modules declare static command indexes for health checks, logs, settings, brokered actions, and open/detail actions. |
| Smoke and publish scripts exist | `scripts/smoke.ps1` and `scripts/publish-windows.ps1` provide reproducible local checks and Windows publish output. The publish script clears `artifacts\release\win-x64` before copying package content so stale modules cannot remain in the portable zip. |
| Release notes generation exists | `scripts/release-notes.ps1` reads `CHANGELOG.md`, records the Windows zip path, SHA256, size, verification commands, and external requirements, and `scripts/publish-windows.ps1` runs it after packaging. |
| CI workflow exists | `.github/workflows/ci.yml` runs restore, build, tests, module validation, module contract validation, package trust hook refresh/strict verification, UI gate, module UI contract snapshots, Shell UI snapshot matrix, template validation, Runner once, smoke, Windows publish, and artifact upload on Windows. |
| Production README exists | `README.md` documents architecture, requirements, build/test commands, runtime commands, publish output, package install/uninstall, module authoring, and troubleshooting. |
| Portable app installer scripts exist | `scripts/install-windows.ps1` and `scripts/uninstall-windows.ps1` install the extracted Windows portable app under `%LOCALAPPDATA%\Programs\MyPowerTools`, manage shortcuts, optional autostart, optional Runner launch, and support `-DryRun` for release validation. |
| Module templates are validated | `templates/` contains `dotnet-inproc-module`, `dotnet-grpc-sidecar-module`, `python-grpc-sidecar-module`, `http-facade-module`, `webview-module`, and `stdio-compat-module`. `scripts/validate-templates.ps1` validates manifests, UI surfaces, .NET builds, and Python syntax. |
| Smoke script fails truthfully | `scripts/smoke.ps1` wraps native commands with `Invoke-Native` and now stops on failed `dotnet`, template, validation, or CLI commands. |
| Package store file operations are retry-hardened | `PackageStore` retries transient Windows directory move/delete failures during install, uninstall, and rollback; package lifecycle tests and strict smoke passed after the hardening. |
| Windows portable artifact exists | `artifacts/release/MyPowerTools-win-x64.zip` was produced by `scripts/publish-windows.ps1` on 2026-07-04. |
| Phase ledger exists | `docs/PHASES.md`, `docs/PROJECT_STATUS.md`, `docs/PHASE_HISTORY.md`, `docs/OPEN_BLOCKERS.md`, `docs/EXTERNAL_VALIDATION.md`, and `.codex/project-state.json` now track the active 9-phase objective, latest P0 validation results, first incomplete phase, external validation needs, and production closure state. |

## Latest Local Verification

Run date: 2026-07-04.

| Command | Result |
|---|---|
| `dotnet --version` | `10.0.301` |
| `dotnet restore MyPowerTools.slnx` | Succeeded; all projects restored or up-to-date. |
| `dotnet build MyPowerTools.slnx` | Succeeded, 0 warnings, 0 errors. |
| `dotnet build MyPowerTools.slnx --no-restore` | Succeeded, 0 warnings, 0 errors. |
| `dotnet test MyPowerTools.slnx --no-build` | Passed 80, failed 0, skipped 0. |
| `dotnet run --project src\MyPowerTools.Cli -- validate modules` | 5 production packages valid. |
| `dotnet run --project src\MyPowerTools.Cli -- validate contracts` | 5 production packages and 7 modules passed manifest, dashboard, settings, command, health, and logs contract checks; AndroidTools notifications and remote commands run through powertoold, and process monitor reports its watch-list degraded state. |
| `dotnet run --project src\MyPowerTools.Cli -- package sign-local modules` | Wrote `shared/package.signature.json` for all 5 production packages and refreshed their hash manifests. |
| `dotnet run --project src\MyPowerTools.Cli -- package trust modules --strict` | Reported `signature-hook` for all 5 production packages. |
| `dotnet run --project src\MyPowerTools.Cli -- ui check modules` | UI gate passed. |
| `dotnet run --project src\MyPowerTools.Cli -- ui snapshot --surface dashboard-card --theme light --size 1366x768 --density normal --out artifacts\ui-snapshots` | Wrote `artifacts/ui-snapshots/ui-snapshot-manifest.json` with 7 dashboard-card contract snapshots and 7 PNG pixel snapshots. First snapshot: 1366x768, 21 unique colors, 876888 non-background pixels. |
| `dotnet run --project src\MyPowerTools.Cli -- ui shell-snapshot --theme light --size 1366x768 --density normal --out artifacts\shell-ui-snapshots` | Wrote `artifacts/shell-ui-snapshots/shell-ui-snapshot-manifest.json` with 10 Shell surface snapshots, 10 PNG pixel snapshots, 8 required Shell surfaces, 12 keyboard shortcuts, 7 focus states, Settings conflict, Command Palette permission-required, and Logs streaming states. |
| `dotnet run --project src\MyPowerTools.Cli -- runner autostart status` | Reports the current HKCU Run autostart state for `MyPowerTools.Runner` through `AutostartBroker` and appends broker audit entries. |
| `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\validate-templates.ps1` | 6 templates passed manifest validation, UI gate, .NET template builds, and Python syntax compilation. |
| `dotnet run --project src\MyPowerTools.Cli -- inspect modules` | 5 packages and 7 modules indexed; AndroidTools modules show `package-runtime:100`; output includes capabilities, required/optional capability requirements, `apply-portproxy` broker permission, and `restart-service` broker permission. |
| `dotnet run --project src\MyPowerTools.Cli -- module list --include-disabled` | Lists all 7 modules with enabled/disabled state. |
| `dotnet run --project src\MyPowerTools.Cli -- diagnostics` | Reports Runner `0.2.0`, HostControl protocol `1.0`, module protocol `1.0`, 5 packages, 7 modules, 81 commands, event seq, runtime paths, transport registration, per-module diagnostics, and `grpc-ipc` process pool `package:android-tools-suite:runtime:powertoold` with all three AndroidTools modules. |
| `dotnet run --project src\MyPowerTools.Runner -- --once` | 7 modules indexed; AdbForwarder, AndroidTools Notifications, AndroidTools Remote Commands, and ADB-backed diagnostics are runnable; AndroidTools modules are served by powertoold where available; Doubao Agent is degraded with 1/3 services reachable; AndroidTools Process Monitor is degraded until a watch list is saved; ScreenEase is degraded on the current monitor because DDC/CI capabilities are unsupported; SmartBird is degraded until Energy Server and FNB-58 are configured. |
| `dotnet run --project src\MyPowerTools.Cli -- run adb-forwarder.diagnostics.summary` | Succeeded with redacted ADB path, redacted ADB device identifiers, and Windows portproxy diagnostics. |
| `dotnet run --project src\MyPowerTools.Cli -- run adb-forwarder.portproxy.plan` | Succeeded with structured current Windows `portproxy` rules, empty default desired mappings, warnings, and no planned changes. |
| `dotnet run --project src\MyPowerTools.Cli -- run doubao-agent.health.check` | Succeeded with typed degraded status: 1/3 Doubao services reachable; planner 38102 and tool 38080 returned HTTP 404, MCP 38189 returned HTTP 200. |
| `dotnet run --project src\MyPowerTools.Cli -- run doubao-agent.self-test` | Succeeded with settings schema availability, redacted `%LOCALAPPDATA%` paths, planner/tool/MCP endpoint list, and token/secret/password redaction proof. |
| `dotnet run --project src\MyPowerTools.Cli -- run smartbird-thermostat.status.summary` | Succeeded with SmartBird HTTP status reachable, ADB identifiers redacted, local paths redacted, and degraded Energy Server/FNB-58 diagnostics. |
| `dotnet run --project src\MyPowerTools.Cli -- run smartbird-thermostat.events.list` | Succeeded with bounded event output: latest 25 of 200 events and `truncated=true`. |
| `dotnet run --project src\MyPowerTools.Cli -- run smartbird-thermostat.service.restart` | Returned expected `permission-required: Broker approval required for service.restart.` with ServiceBroker details. |
| `dotnet run --project src\MyPowerTools.Cli -- run screenease.status.summary` | Succeeded with active profile, 1 detected Windows display, default profiles/rules, and DDC/CI writer status `unsupported` on the current Generic PnP Monitor. |
| `dotnet run --project src\MyPowerTools.Cli -- run screenease.native-writer.status` | Succeeded with `nativeHost.available=false` and an actionable `GetMonitorCapabilities` unsupported-monitor diagnostic from the Windows DDC/CI provider. |
| `dotnet run --project src\MyPowerTools.Cli -- run screenease.profile.apply` | Succeeded with profile application plan and safe default `native-host-required` status because hardware writes are disabled unless `hardwareWrite=true` is passed or the native writer is configured on. |
| `dotnet run --project src\MyPowerTools.Cli -- run adb-forwarder.portproxy.apply` | Returns `permission-required: Broker approval required for network.portproxy.apply.` with exit code 1. Dynamic runtime tests verify the detailed request payload. |
| `dotnet run --project src\MyPowerTools.Cli -- broker portproxy list` | Succeeded and listed 6 local Windows portproxy rule(s). |
| `dotnet run --no-build --project src\MyPowerTools.Cli -- broker portproxy apply --listen-address 127.0.0.1 --listen-port 45678 --connect-address 127.0.0.1 --connect-port 45679` | Returned `permission-required` in normal user context, proving the CLI broker path does not fake elevated success. |
| `dotnet run --project src\MyPowerTools.Cli -- broker secret self-test --module cli.secret-self-test --name self-test-codex` | Succeeded through Windows Credential Manager; saved, read-verified, deleted, and delete-verified a temporary secret reference without printing the secret value. |
| Release Shell smoke | Release Shell connected to Runner `0.2.0`, reported 7 modules, 7 dashboard cards, 81 commands, requested Runner shutdown, and the release Runner exited cleanly. |
| Published package trust verification | `artifacts\release\win-x64\Cli\MyPowerTools.Cli.exe package trust artifacts\release\win-x64\modules --strict` reported `signature-hook` for all 5 production packages. |
| Published Runner release-root discovery | `artifacts\release\win-x64\Runner\MyPowerTools.Runner.exe --once --data-root artifacts\release-root-once-data` indexed 7 modules from `artifacts\release\win-x64\modules` without an explicit `--modules` argument. |
| Published Runner/Shell HostControl smoke | `artifacts\release\win-x64\Runner\MyPowerTools.Runner.exe` plus `artifacts\release\win-x64\Shell\MyPowerTools.Shell.Avalonia.exe --smoke --timeout-ms 30000 --quit-runner` connected to Runner `0.2.0`, reported 7 modules, 7 dashboard cards, 67 commands, requested Runner shutdown, and the release Runner exited cleanly. |
| Published CLI autostart dry-run | `artifacts\release\win-x64\Cli\MyPowerTools.Cli.exe runner autostart enable --dry-run` resolved the sibling release Runner command. |
| Published CLI secret self-test | `artifacts\release\win-x64\Cli\MyPowerTools.Cli.exe broker secret self-test --module cli.secret-self-test --name self-test-release-codex` passed through Windows Credential Manager without printing a secret value. |
| Published CLI permission inspection | `artifacts\release\win-x64\Cli\MyPowerTools.Cli.exe inspect modules` printed module capabilities, required/optional capability requirements, `apply-portproxy`, and `restart-service` broker permissions. |
| `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\publish-windows.ps1` | Produced `artifacts/release/MyPowerTools-win-x64.zip` with SHA256 `2418C0BEF33FE955DB81C677B1C74FE3E0DEC5CCA4D2D14BD1CB5D46B02DFDB0` and size 171430107 bytes. |
| `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\release-notes.ps1` | Produced `artifacts/release/RELEASE_NOTES.md` with artifact path, SHA256, size, verification commands, and external requirements. |
| `artifacts\release\win-x64\Runner\MyPowerTools.Runner.exe --once --data-root artifacts\release-root-once-data-screenease-writer` | Release Runner indexed 7 modules from the release root and started AndroidTools powertoold from `modules/android-tools-suite/windows/x64`. |
| Release Shell smoke | Release Shell connected to Runner `0.2.0`, reported 7 modules, 7 dashboard cards, 81 commands, and requested Runner shutdown. |
| `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\install-windows.ps1 -PackageRoot artifacts\release\win-x64 -InstallDir artifacts\install-dryrun -DryRun` | Succeeded and printed the install plan for the portable release root. |
| `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\uninstall-windows.ps1 -InstallDir artifacts\install-dryrun -DryRun -Force` | Succeeded and printed the uninstall plan without removing files. |

## Open Production Criteria

| Area | Remaining Work |
|---|---|
| Runner | Broader ModuleSupervisor health policy automation remains open. HostControl graceful shutdown, brokered autostart, tray integration, release-root discovery, typed runtime diagnostics, gRPC runtime process telemetry, manual runtime pool restart controls, persisted pause/resume restart-policy controls, and expiring maintenance windows now exist and are verified by tests/smoke. |
| Shell | P4 internal closure is complete. Core Dashboard, Modules, schema-rendered Settings, Logs, Notifications, Packages, Diagnostics, command palette, broker permission prompt, broker audit, module permission visibility, module enable/disable, HostControl reconnection, HostControl event stream resume/refresh handling, keyboard navigation, centralized theme tokens, deterministic Shell surface snapshots, and Package Manager install/repair/uninstall/rollback actions exist and are verified. |
| ModuleHost | InProc, gRPC IPC, and stdio execution are wired into Runtime; gRPC sidecar crash restart, package shared runtime pooling, process-level runtime pool telemetry, persisted pause/resume restart policy handling, and expiring maintenance windows are verified; AndroidTools now has a production package-shared powertoold sidecar. Additional production sidecars for other tools remain future hardening where their current InProc/HTTP facades are sufficient. |
| Existing tools | P2 internal closure is complete. Current manifests, static command indexes, facades, HTTP compatibility paths, and degraded states are in place; AdbForwarder has a runnable InProc diagnostics module plus a managed port mapping/rollback request model. AndroidTools has a package-shared powertoold T2 sidecar for command import/execution, notification diagnostics, process watch scanning, invalid notification config degradation, and empty watch-list degradation, with connected-device notification/command flows tracked as external validation. ScreenEase has a runnable InProc profile/display facade and Windows DDC/CI native display writer; current hardware validation remains external because this monitor reports unsupported capabilities. Doubao Agent has an InProc controller facade with planner/tool/MCP separation, role-specific degradation tests, self-test, settings schema, and log summary. SmartBird has an InProc typed facade for status/events/config/logs/restart plus degraded hardware diagnostics; full Energy Server/FNB-58 validation remains external. |
| Broker | P3 internal closure is complete. Portproxy changes have an audited NetworkBroker execution path, rollback, HostControl audit exposure, Shell visibility, planned permission-level enforcement, `serviceUser` ServiceBroker audit, brokered Runner autostart status/enable/disable, and Credential Manager-backed SecretBroker save/read/delete. A separated elevated helper/service process remains external because it requires administrator context or signed helper packaging. |
| Packaging | MSI/MSIX or package-manager distribution, package update metadata, and external private-key signing remain open. Local hash manifests, trust hooks, HostControl package operations, rollback exclusion from discovery, and portable install/uninstall scripts exist. |
| Testing | UI contract + PNG pixel snapshots, Shell keyboard/focus snapshot metadata, module contract tests, package rollback tests, HTTP facade integration tests, P2 module degraded/facade tests, gRPC sidecar crash policy/process-diagnostics/restart-control/pause-resume/persistence/expiry tests, Runtime module-state tests, HostControl module-state tests, and CLI process tests exist, including CLI diagnostics process rows and runner process restart/pause/resume for active gRPC sidecar pools. |
| Release | Signed installer/package-manager distribution remains open; self-contained Windows publish, portable zip, portable install/uninstall scripts, CI workflow, README, and release notes generation exist. |

## Current Validation Commands

```powershell
dotnet restore MyPowerTools.slnx
dotnet build MyPowerTools.slnx
dotnet test MyPowerTools.slnx --no-build
dotnet run --project src\MyPowerTools.Cli -- validate modules
dotnet run --project src\MyPowerTools.Cli -- validate contracts
dotnet run --project src\MyPowerTools.Cli -- package sign-local modules
dotnet run --project src\MyPowerTools.Cli -- package trust modules --strict
dotnet run --project src\MyPowerTools.Cli -- ui check modules
dotnet run --project src\MyPowerTools.Cli -- module list --include-disabled
dotnet run --project src\MyPowerTools.Cli -- diagnostics
dotnet run --project src\MyPowerTools.Cli -- ui snapshot --surface dashboard-card --theme light --size 1366x768 --density normal --out artifacts\ui-snapshots
dotnet run --project src\MyPowerTools.Cli -- ui shell-snapshot --theme light --size 1366x768 --density normal --out artifacts\shell-ui-snapshots
dotnet run --project src\MyPowerTools.Cli -- runner autostart status
dotnet run --project src\MyPowerTools.Cli -- broker secret self-test
dotnet run --project src\MyPowerTools.Runner -- --once
pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\validate-templates.ps1
pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\smoke.ps1
pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\publish-windows.ps1
pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\install-windows.ps1 -PackageRoot artifacts\release\win-x64 -InstallDir artifacts\install-dryrun -DryRun
pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\uninstall-windows.ps1 -InstallDir artifacts\install-dryrun -DryRun -Force
```
