# MyPowerTools Phase Ledger

Run date: 2026-07-04.

The active objective uses the 9-phase model below. This ledger maps the current repository evidence to each phase and selects the first incomplete phase as the next execution target.

## Phase Status

| Phase | Status | Latest Verified Result | Evidence Files | Validation Commands | Known Gaps | Next Allowed Work |
|---|---|---|---|---|---|---|
| P0 State audit and project ledger | done | Phase docs and `.codex/project-state.json` created after current repository, plan package, production evidence, handoff, release notes, and validation outputs were inspected. | `docs/PHASES.md`, `docs/PROJECT_STATUS.md`, `docs/PHASE_HISTORY.md`, `docs/OPEN_BLOCKERS.md`, `docs/EXTERNAL_VALIDATION.md`, `.codex/project-state.json` | `dotnet --version`; `dotnet restore MyPowerTools.slnx`; `dotnet build MyPowerTools.slnx --no-restore`; `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --project src\MyPowerTools.Cli -- validate modules`; `dotnet run --project src\MyPowerTools.Cli -- validate contracts`; `dotnet run --project src\MyPowerTools.Cli -- ui check modules`; `dotnet run --project src\MyPowerTools.Runner -- --once`; `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\smoke.ps1` | None for P0. | Complete. |
| P1 Foundation and architecture conformance | done | Runner/Shell split, HostControl typed IPC, transport-tiered runtime, settings revision handling, event sequence handling, command idempotency, diagnostics, package registry, command index, and no tool-specific ID leakage in foundation projects are verified. | `src/MyPowerTools.Runner`, `src/MyPowerTools.Shell.Avalonia`, `src/MyPowerTools.HostControl`, `src/MyPowerTools.Runtime`, `src/MyPowerTools.Protocol`, `src/MyPowerTools.ModuleHost.*`, `docs/PRODUCTION_READINESS.md` | `dotnet build MyPowerTools.slnx --no-restore`; `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --project src\MyPowerTools.Cli -- validate modules`; `dotnet run --project src\MyPowerTools.Cli -- validate contracts`; `dotnet run --project src\MyPowerTools.Runner -- --once`; `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\smoke.ps1`; `rg -n "android-tools|screenease|adb-forwarder|doubao-agent|smartbird" <foundation paths>` | None for P1; ModuleSupervisor policy automation is complete under P5. | Complete. |
| P2 Module runtime and existing tools production closure | done | 5 production packages and 7 modules validate; Runner indexes all modules; AndroidTools uses package-shared `powertoold` gRPC IPC as T2 primary for its three modules with InProc fallback retained. ScreenEase, AdbForwarder, Doubao Agent, and SmartBird have real module paths with truthful degraded states where external dependencies are absent. New acceptance coverage verifies AndroidTools invalid notification config, AndroidTools empty process watch list, and Doubao role-specific partial outage behavior. | `modules/`, `src/AndroidTools.MyPowerTools`, `src/AndroidTools.Powertoold`, `src/ScreenEase.MyPowerTools`, `src/MyPowerTools.Platform.Windows`, `src/AdbForwarder.MyPowerTools`, `src/DoubaoAgent.MyPowerTools`, `src/SmartBirdThermostat.MyPowerTools`, `src/MyPowerTools.Tests/RuntimeAcceptanceTests.cs`, `docs/PRODUCTION_READINESS.md` | `dotnet build MyPowerTools.slnx --no-restore`; `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --no-build --project src\MyPowerTools.Cli -- validate modules`; `dotnet run --no-build --project src\MyPowerTools.Cli -- validate contracts`; `dotnet run --no-build --project src\MyPowerTools.Cli -- diagnostics`; `dotnet run --no-build --project src\MyPowerTools.Cli -- inspect modules`; `dotnet run --no-build --project src\MyPowerTools.Cli -- module list --include-disabled`; `dotnet run --no-build --project src\MyPowerTools.Runner -- --once`; `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\smoke.ps1` | External validation remains for ScreenEase DDC/CI hardware writes, SmartBird Energy Server/FNB-58 hardware, Doubao documented production endpoint contracts, and Android connected-device notification/command flows. | Continue with P3 broker, privilege, security and secrets. |
| P3 Broker, privilege, security and secrets | done | Broker audit, NetworkBroker portproxy planning/execution/rollback path, SecretBroker Credential Manager path, AutostartBroker, ServiceBroker `serviceUser` audit, HostControl permission fields, Shell permission visibility, CLI inspection, and planned permission levels are tested. | `src/MyPowerTools.Broker`, `src/MyPowerTools.Platform.*`, `modules/*/module.json`, `schemas/module.schema.json`, `src/MyPowerTools.Tests/RuntimeAcceptanceTests.cs`, `docs/08-security-privilege-data.md`, `docs/PRODUCTION_READINESS.md` | `dotnet build MyPowerTools.slnx --no-restore`; `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --no-build --project src\MyPowerTools.Cli -- inspect modules`; `dotnet run --no-build --project src\MyPowerTools.Cli -- broker audit`; `dotnet run --no-build --project src\MyPowerTools.Cli -- broker portproxy list`; `dotnet run --no-build --project src\MyPowerTools.Cli -- broker portproxy apply ...`; `dotnet run --no-build --project src\MyPowerTools.Cli -- run adb-forwarder.portproxy.apply`; `dotnet run --no-build --project src\MyPowerTools.Cli -- broker secret self-test`; `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\smoke.ps1` | Live elevated helper/service execution still requires administrator context or signed helper and is tracked as external. Production signing remains P6/external. | Continue with P4 Shell UI, design system and visual quality. |
| P4 Shell UI, design system and visual quality | done | HostControl-backed Shell pages, centralized theme tokens, keyboard navigation, schema-rendered Settings, module UI gate, module PNG snapshots, Shell surface snapshots with keyboard/focus metadata, strict smoke, and release Shell smoke are verified. | `src/MyPowerTools.Shell.Avalonia`, `src/MyPowerTools.UI`, `ui/`, `schemas/ui-surface.schema.json`, `artifacts/ui-snapshots`, `artifacts/shell-ui-snapshots`, `src/MyPowerTools.Tests/RuntimeAcceptanceTests.cs` | `dotnet build MyPowerTools.slnx --no-restore`; `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --project src\MyPowerTools.Cli -- ui check modules`; `dotnet run --project src\MyPowerTools.Cli -- ui snapshot --surface dashboard-card --theme light --size 1366x768 --density normal --out artifacts\ui-snapshots`; `dotnet run --project src\MyPowerTools.Cli -- ui shell-snapshot --theme light --size 1366x768 --density normal --out artifacts\shell-ui-snapshots`; `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\smoke.ps1`; release Runner/Shell smoke | None internally. Future richer module editors can be added as feature work after the schema-backed Settings/Detail surfaces. | Complete. |
| P5 Reliability, observability and runtime policy | done | ModuleSupervisor now records module health observations, consecutive failures, supervisor state, and actionable next steps; RuntimeDiagnostics, HostControl, CLI, Shell Diagnostics, Dashboard alerts, runner process policy shorthand, smoke, and release Shell smoke are verified. | `src/MyPowerTools.Runtime/ModuleSupervisor.cs`, `src/MyPowerTools.Runtime`, `proto/mpt_host_control_v1.proto`, `src/MyPowerTools.HostControl`, `src/MyPowerTools.Cli`, `src/MyPowerTools.Shell.Avalonia`, `src/MyPowerTools.Tests/RuntimeAcceptanceTests.cs`, `docs/PRODUCTION_READINESS.md` | `dotnet build MyPowerTools.slnx --no-restore`; `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --no-build --project src\MyPowerTools.Cli -- diagnostics`; `dotnet run --no-build --project src\MyPowerTools.Cli -- runner process pause . --duration-minutes 1`; `dotnet run --no-build --project src\MyPowerTools.Cli -- validate modules`; `dotnet run --no-build --project src\MyPowerTools.Cli -- validate contracts`; `dotnet run --no-build --project src\MyPowerTools.Runner -- --once`; `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\smoke.ps1`; release Runner/Shell smoke | None internally. External service and hardware availability remains tracked under existing external validation rows. | Complete. |
| P6 Packaging, templates, CLI, install and release | done | CLI, package hash/trust, install/uninstall/update/rollback/repair, templates, smoke, publish script, release notes, release/update metadata, Scoop manifest, portable install/uninstall dry-run, CI workflow, release Runner/Shell smoke, and release autostart dry-run are verified. Latest release hash is `EAA7E82DCC8B7BA63307360402C68A6764AFCA3870E1703C8FAE0EF5BE1266A4`. | `src/MyPowerTools.Cli`, `src/MyPowerTools.Packaging`, `templates/`, `scripts/`, `.github/workflows/ci.yml`, `artifacts/release/RELEASE_NOTES.md`, `artifacts/release/release-metadata.json`, `artifacts/release/package-managers/scoop/mypowertools.json` | `dotnet restore MyPowerTools.slnx`; `dotnet build MyPowerTools.slnx --no-restore`; `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --no-build --project src\MyPowerTools.Cli -- validate modules`; `dotnet run --no-build --project src\MyPowerTools.Cli -- validate contracts`; `dotnet run --no-build --project src\MyPowerTools.Cli -- package trust modules --strict`; `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\validate-templates.ps1`; `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\smoke.ps1`; `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\publish-windows.ps1`; release package trust; release Runner/Shell smoke; release CLI autostart dry-run; install/uninstall dry-run; release hash and zip hygiene checks | Production private-key signing and channel publication require external signing material/service. | Complete. |
| P7 Cross-platform capability packs and degraded behavior | partial | Windows provider is production-focused; macOS/Linux platform packs compile and return explicit degraded responses for unsupported providers; capabilities and requirements are visible in CLI and Shell. | `src/MyPowerTools.Platform.Abstractions`, `src/MyPowerTools.Platform.Windows`, `src/MyPowerTools.Platform.Mac`, `src/MyPowerTools.Platform.Linux`, `docs/04-platform-capabilities.md` | `dotnet build MyPowerTools.slnx --no-restore`; `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --project src\MyPowerTools.Cli -- inspect modules`; `dotnet run --project src\MyPowerTools.Cli -- diagnostics`; `dotnet run --project src\MyPowerTools.Cli -- validate contracts` | macOS/Linux runtime validation on native hosts and UDS end-to-end smoke remain. | Add native cross-platform validation and UDS coverage. |
| P8 Final production closure | not-started | Final closure cannot begin until P2-P7 internal gaps are resolved or reclassified with authoritative evidence. | `docs/PROJECT_STATUS.md`, `docs/OPEN_BLOCKERS.md`, `docs/EXTERNAL_VALIDATION.md`, `docs/PRODUCTION_READINESS.md` | Full final validation matrix from the objective. | Open internal phase gaps remain. | Start after P2-P7 reach done or externally blocked. |

## Current Phase Selection

P0, P1, P2, P3, P4, P5, and P6 are complete against current local evidence. The next executable phase is P7.
