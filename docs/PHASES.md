# MyPowerTools Phase Ledger

Run date: 2026-07-04.

The active objective uses the 9-phase model below. This ledger maps the current repository evidence to each phase and selects the first incomplete phase as the next execution target.

## Phase Status

| Phase | Status | Latest Verified Result | Evidence Files | Validation Commands | Known Gaps | Next Allowed Work |
|---|---|---|---|---|---|---|
| P0 State audit and project ledger | done | Phase docs and `.codex/project-state.json` created after current repository, plan package, production evidence, handoff, release notes, and validation outputs were inspected. | `docs/PHASES.md`, `docs/PROJECT_STATUS.md`, `docs/PHASE_HISTORY.md`, `docs/OPEN_BLOCKERS.md`, `docs/EXTERNAL_VALIDATION.md`, `.codex/project-state.json` | `dotnet --version`; `dotnet restore MyPowerTools.slnx`; `dotnet build MyPowerTools.slnx --no-restore`; `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --project src\MyPowerTools.Cli -- validate modules`; `dotnet run --project src\MyPowerTools.Cli -- validate contracts`; `dotnet run --project src\MyPowerTools.Cli -- ui check modules`; `dotnet run --project src\MyPowerTools.Runner -- --once`; `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\smoke.ps1` | None for P0. | Continue with P2 because P1 is already verified complete. |
| P1 Foundation and architecture conformance | done | Runner/Shell split, HostControl typed IPC, transport-tiered runtime, settings revision handling, event sequence handling, command idempotency, diagnostics, package registry, command index, and no tool-specific ID leakage in foundation projects are verified. | `src/MyPowerTools.Runner`, `src/MyPowerTools.Shell.Avalonia`, `src/MyPowerTools.HostControl`, `src/MyPowerTools.Runtime`, `src/MyPowerTools.Protocol`, `src/MyPowerTools.ModuleHost.*`, `docs/PRODUCTION_READINESS.md` | `dotnet build MyPowerTools.slnx --no-restore`; `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --project src\MyPowerTools.Cli -- validate modules`; `dotnet run --project src\MyPowerTools.Cli -- validate contracts`; `dotnet run --project src\MyPowerTools.Runner -- --once`; `pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts\smoke.ps1`; `rg -n "android-tools|screenease|adb-forwarder|doubao-agent|smartbird" <foundation paths>` | Broader ModuleSupervisor policy automation is tracked under P5, not a P1 gate. | Continue with P2 module production closure. |
| P2 Module runtime and existing tools production closure | next | 5 production packages and 7 modules validate; Runner indexes all modules; AndroidTools, ScreenEase, AdbForwarder, Doubao Agent, and SmartBird have real module paths with current degraded states where external dependencies are absent. Doubao Agent now has an InProc controller module with planner/tool/MCP health separation, settings schema, self-test, log summary, and degraded status when only part of the runtime is reachable. | `modules/`, `src/AndroidTools.MyPowerTools`, `src/ScreenEase.MyPowerTools`, `src/AdbForwarder.MyPowerTools`, `src/DoubaoAgent.MyPowerTools`, `docs/PRODUCTION_READINESS.md` | `dotnet run --project src\MyPowerTools.Cli -- validate modules`; `dotnet run --project src\MyPowerTools.Cli -- validate contracts`; `dotnet run --project src\MyPowerTools.Cli -- inspect modules`; `dotnet run --project src\MyPowerTools.Cli -- module list --include-disabled`; module command probes listed in `docs/PROJECT_STATUS.md` | AndroidTools long-running `powertoold` T2 parity, ScreenEase native display writer, SmartBird config/events/restart coverage, and real Doubao endpoint contract validation remain. | Complete module-specific production behavior and degraded-path tests. |
| P3 Broker, privilege, security and secrets | partial | Broker audit, NetworkBroker portproxy planning/execution path, SecretBroker Credential Manager path, AutostartBroker, HostControl permission fields, Shell permission visibility, and CLI inspection are present. | `src/MyPowerTools.Broker`, `src/MyPowerTools.Platform.*`, `modules/*/module.json`, `docs/08-security-privilege-data.md`, `docs/PRODUCTION_READINESS.md` | `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --project src\MyPowerTools.Cli -- inspect modules`; `dotnet run --project src\MyPowerTools.Cli -- broker audit`; `dotnet run --project src\MyPowerTools.Cli -- broker portproxy list`; `dotnet run --project src\MyPowerTools.Cli -- run adb-forwarder.portproxy.apply` | Separated elevated helper/service and production signing path remain external or release-track work. | Harden elevated helper packaging and expand broker docs/tests where needed. |
| P4 Shell UI, design system and visual quality | partial | HostControl-backed Shell pages, token/component foundation, module UI gate, module PNG snapshots, and Shell surface snapshots exist. | `src/MyPowerTools.Shell.Avalonia`, `src/MyPowerTools.UI`, `ui/`, `schemas/ui-surface.schema.json`, `artifacts/ui-snapshots`, `artifacts/shell-ui-snapshots` | `dotnet run --project src\MyPowerTools.Cli -- ui check modules`; `dotnet run --project src\MyPowerTools.Cli -- ui snapshot --surface dashboard-card --theme light --size 1366x768 --density normal --out artifacts\ui-snapshots`; `dotnet run --project src\MyPowerTools.Cli -- ui shell-snapshot --theme light --size 1366x768 --density normal --out artifacts\shell-ui-snapshots`; Shell smoke | Interactive screenshot diff matrix, keyboard navigation pass, and module-specific deep editors remain. | Build stronger Shell visual/keyboard coverage and deep editor surfaces. |
| P5 Reliability, observability and runtime policy | partial | Sidecar crash recovery, restart throttling, restart controls, pause/resume, policy persistence, expiry, runtime diagnostics, log redaction, and Shell event stream reconnect are tested. | `src/MyPowerTools.Runtime`, `src/MyPowerTools.ModuleHost.GrpcIpc`, `src/MyPowerTools.HostControl`, `src/MyPowerTools.Shell.Avalonia`, `src/MyPowerTools.Tests/RuntimeAcceptanceTests.cs` | `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --project src\MyPowerTools.Cli -- diagnostics`; `dotnet run --project src\MyPowerTools.Runner -- --once`; smoke | Broader ModuleSupervisor health policy automation and additional release smoke coverage remain. | Extend runtime failure policy and diagnostics coverage. |
| P6 Packaging, templates, CLI, install and release | partial | CLI, package hash/trust, install/uninstall/rollback/repair, templates, smoke, publish script, release notes, portable install/uninstall dry-run, and CI workflow exist. | `src/MyPowerTools.Cli`, `src/MyPowerTools.Packaging`, `templates/`, `scripts/`, `.github/workflows/ci.yml`, `artifacts/release/RELEASE_NOTES.md` | `scripts/validate-templates.ps1`; `scripts/publish-windows.ps1`; install/uninstall dry-run; release Runner/Shell smoke; release CLI autostart dry-run | MSI/MSIX or package-manager distribution, update metadata, and external private-key signing remain. | Finish signed installer/package-manager path and update metadata. |
| P7 Cross-platform capability packs and degraded behavior | partial | Windows provider is production-focused; macOS/Linux platform packs compile and return explicit degraded responses for unsupported providers; capabilities and requirements are visible in CLI and Shell. | `src/MyPowerTools.Platform.Abstractions`, `src/MyPowerTools.Platform.Windows`, `src/MyPowerTools.Platform.Mac`, `src/MyPowerTools.Platform.Linux`, `docs/04-platform-capabilities.md` | `dotnet build MyPowerTools.slnx --no-restore`; `dotnet test MyPowerTools.slnx --no-build`; `dotnet run --project src\MyPowerTools.Cli -- inspect modules`; `dotnet run --project src\MyPowerTools.Cli -- diagnostics`; `dotnet run --project src\MyPowerTools.Cli -- validate contracts` | macOS/Linux runtime validation on native hosts and UDS end-to-end smoke remain. | Add native cross-platform validation and UDS coverage. |
| P8 Final production closure | not-started | Final closure cannot begin until P2-P7 internal gaps are resolved or reclassified with authoritative evidence. | `docs/PROJECT_STATUS.md`, `docs/OPEN_BLOCKERS.md`, `docs/EXTERNAL_VALIDATION.md`, `docs/PRODUCTION_READINESS.md` | Full final validation matrix from the objective. | Open internal phase gaps remain. | Start after P2-P7 reach done or externally blocked. |

## Current Phase Selection

P0 is complete and P1 is already verified complete against current evidence. The next executable phase is P2.
