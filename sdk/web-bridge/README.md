# MyPowerTools Web Bridge

Install `@mypowertools/web-bridge`, import `mpt`, then use `invoke`, settings,
Secret Store, event publishing, external navigation, and theme notifications.
The bridge falls back to `window.parent.postMessage` for browser development.
