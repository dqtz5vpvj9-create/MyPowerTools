# HTTP Facade Module Template

Run the sample service:

```powershell
python .\server.py
```

Validate from the repository root:

```powershell
dotnet run --project src\MyPowerTools.Cli -- validate templates\http-facade-module
dotnet run --project src\MyPowerTools.Cli -- ui check templates\http-facade-module
```
